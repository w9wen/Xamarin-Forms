<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Forms.DataAccess.TodoItemPage">
    <ContentPage.Content>
        <StackLayout Margin="20" VerticalOptions="StartAndExpand">
            <Label Text="Name"/>
            <Entry Text="{Binding Name}"/>
            <Label Text="Notes"/>
            <Entry Text="{Binding Notes}"/>
            <Label Text="Done"/>
            <Switch IsToggled="{Binding Done}"/>
            <Button x:Name="Button_Save" Text="Save" Clicked="Button_Save_Clicked"/>
            <Button x:Name="Button_Delete" Text="Delete" Clicked="Button_Delete_Clicked"/>
            <Button x:Name="Button_Cancel" Text="Cancel" Clicked="Button_Cancel_Clicked"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>